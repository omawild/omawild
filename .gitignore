# Theme exports / backups — large binaries, never in git
*.zip

# Shopify CLI
.shopify/
node_modules/

# Preview/QA artefacts. main syncs to the production theme via Shopify's GitHub
# integration, so stray files at the repo root end up on the store.
# Root-level only: assets/*.png are real theme files and must stay tracked.
.playwright-mcp/
/*.png

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.swp

# Local-only CLI env (store domains, tokens)
.env
.env.*
!.env.example

# Source HTML exports for the landing-page import (see docs/ow-asset-manifest.md)
omawild-landing*.html
neue-haas-grotesk-display-pro.zip
